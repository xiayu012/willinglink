# WillingLink — Claude 工作须知

> 每次新会话先读 `.claude/PROJECT_STATE.md`（项目当前状态单点事实源），再继续。

涉及产品行为设计、纠错或验收，先读 `.claude/DEVELOPMENT_JUDGMENT.md`。Claude 接到 Codex 任务后先对照原始需求检查提案；发现日常反例先反馈，不照单实现错误规则。

**开始任何搜索/入库/AI 相关改动前，先读 `.claude/AGENT_LOG.md`** —— 那是历次
Claude 会话的决策记录（为什么代码长这样、哪些坑已经踩过、哪些代码看似废弃实则
是保留的回退路径）。改动这些区域前不读它，大概率会重复已解决的问题或破坏有意
的设计。

**涉及「AI 能不能替住户给别的住户发消息」的改动前，先读
`docs/USER_FACING_CAPABILITY_TRUTH.md`** —— 那是第三方出站能力的单点事实源：
现在只开放「个人物品使用提醒」一条受约束功能，其余自由文本出站（代转达、催办、
协调、群体联系、cron 主动发起）已按老板 2026-09-12 严格口径全部收回。

速查：
- 产品方向：LLM-native。查询理解集中在 `lib/rental/query-plan.ts` 的
  `planQuery`（运行时与评测共用的唯一入口），正则只做兜底，硬筛选"偏严格"
  （只剔除可证明违反的，**沉默不算违反**），排序交给语义 rerank + 偏好。
- 约束是**集合**不是单值：`cities` / `bedroomsAnyOf` 列了备选就全部列出，
  按 OR 处理。"列了多个所以整条不筛"是已经修掉的老 bug，别改回去。
- `searchRental` 严格模式是默认；旧"换一个"级联**保留勿删**，
  `SEARCH_LEGACY_PICK_ONE=1` 回退。理解层回退：`SEARCH_PLANNER_OFF=1`。
- 与用户沟通用中文；用户要求"先讨论再动手"时不要直接改代码。

## 当前开发协作约定（用户授权）

- Codex 负责分配任务、独立复审与验收；Claude Code 负责主要实现，默认 Sonnet、medium。
- 已授权向 Claude Code 提供本项目相关代码和**脱敏**测试，不必反复询问同一范围。
  不因此扩大到密钥、真人数据外传、真实消息发送或删除数据；工具权限仍照常执行。
- 实施开发改动后，由监督方核验并提交、push 远程 main；不要把未验收说成通过。
- 暂不跑 Twilio 发送链路测试；测试仅用隔离测试屋，不污染真人历史。
- 控制额度：任务范围明确、先读必要文件，避免每一步携带整条长期会话或反复全仓检索。
- `public/sw.js` 是既有用户文件，未经专门要求不要修改或加入本次提交。

## ⚠️ 绝对不许伪造入站消息打进真实住户的对话

**本地测试脚本连的是同一个生产库。** 你在脚本里编一句
「我上周被裁了」去试探 AI 的反应，那句话就会变成用户的**真实对话历史**，
AI 之后会带着这段你凭空编的前情跟他说话——**等于给用户安了一个人设**。
2026-09-02 真的发生过，用户收到的短信里 AI 在跟他讨论他从未提过的失业问题。

这条不靠自觉，**代码里有硬闸**（`lib/chat/coliving/guard.ts`）：

- 服务器进程（Vercel / next dev）随便写
- **本地脚本默认一个字都写不进去**；要写必须同时满足
  `COLIVING_LOCAL_WRITE=1` **且** 目标 household 是测试屋（`is_test = true`）
- 真人住的房子，本地进程**永远**写不进去，设什么变量都没用

要本地跑对话：`pnpm coliving:db --test-house` 开一栋，再带 `COLIVING_LOCAL_WRITE=1`。

**别为了图快去绕过它**（改 guard、直接写 SQL、把 is_test 打开）。
绕过去的代价是用户收到一条基于虚构事实的真短信。

## ⚠️ 轻易不要改大脑提示词；非改不可时，位置比措辞更要紧

**提示词放在哪一层，就等于给它多大权重。**

- `constitution.md` / `core.md` / `craft.md` 是**常驻**，每一轮都读 ——
  往这里加一句，等于让它作用于**所有**对话，包括完全不相关的那些
- 情境模块（`conflict.md` 等）只在命中路由时加载 —— 话题相关的话写这里
- `context.ts` 的「⚠️ 你不知道的事」是**最前面、最高优先级**的位置，
  **只放"绝对不能编造"的硬事实清单，不放任何行为指导**

出过事：为了修「不该默认全屋共用」，我把一整段共用设施的说明写进了
「你不知道的事」。结果 AI 连**跟房东要电话号码**都在念「方便以后协调
厨房、卫生间这些共用的事」——那一轮跟设施毫无关系。
**位置错了，一句正确的话也会污染所有输出。**

改之前先问三句：

1. **能不能不改？** 大多数「它说错了一句」是波动，不是缺规则。
   先看是不是已有条款没被执行（那要修的是审稿清单，不是准则）。
2. **该放哪一层？** 只在某类话题下成立的，就放情境模块。
3. **是加还是删？** 优先删矛盾的旧句子，而不是加新句子压过它。
   规则越堆越互相抵消 —— 这是已经踩过两次的坑。

**宪法只写正面原则**（正面表述、描述行为的原则实证上更有效，见 C3AI）；
**检查逻辑一律写进 `rubric.md`**，那是给批判器用的、可以逐条提问的清单。
把检查塞进宪法会同时毁掉两边。

## ⚠️ 不要替大脑写话术

**发出去的每一个字，都归准则管（`lib/ai/brains/*/doctrine/*.md`）。
代码只负责给事实和约束，不负责给措辞。**

出过事：`context.ts` 里我写了「欢迎一句，说明你是干什么的（一句）」，
结果 AI 对新住户说「我是管理员，谁几点用厨房、垃圾怎么倒、有什么事找谁
都归我协调。你刚安顿下来，先不用管这些」——提了一堆再说不用管，蠢得可笑。
**那不是模型笨，是我用一句随手写的运行时提示，压过了一万三千字打磨过的准则。**

所以：

- 运行时上下文（`context.ts`）、cron 的 brief（`outreach.ts`）里
  **只陈述事实和目标**：这人是谁、你知道什么、要达成什么。
  **不写"你应该说……"、不给例句、不规定语气和结构。**
- 要改 AI 说话的方式，**改 doctrine 的 .md，不改 .ts**。
- 硬编码的固定文案只留给「模型根本没被调用」的路径
  （比如认不出的号码），并且要短、要中性。
- 确定性的格式问题（markdown 剥除、长度截断）用代码兜底 ——
  **提示词管判断，代码管格式**，但代码不管内容。

## ⚠️ 组合优化类的计算，交给代码算，不要靠模型心算

**LLM 的"聪明"体现在语言理解、常识推理、模式匹配上，不是精确计算。**
给定约束条件穷举排列组合、找最优解，这类任务本来就不是语言模型擅长
的事——无论换多贵的模型都一样，它是在"生成看起来合理的文字"，不是
在真的做算术。堆多少条提示词提醒它"要考虑其他排列"，都只能提高它
**想到**要这么做的概率，不能保证它**算对**。

出过事（2026-09-04）：三人厨房时段冲突，模型把唯一有硬约束（18点到家、
要占两小时）的人默认排在最前面，导致唯一说了偏好时间的房东被挤到
20:30 才吃饭——换个排列顺序两人都能满足，但模型没想到，反复补提示词
（core.md 加"结果不合理要承认"）只能让它承认"这不够好"，不能让它算出
更好的排法。改用 `lib/chat/coliving/scheduling.ts`：一段几十行的纯
函数，穷举全部排列、按目标函数打分排序，包成 `pickSchedule` 工具让
模型调用——模型只需要从代码算好的候选里选、判断合不合适、想怎么
措辞，组合推理这一步彻底不用模型自己做。见 AGENT_LOG 对应条目的完整
过程（含一次目标函数设计错误、验证后推翻重来的记录）。

**这不是给这一个场景打的补丁，是往后遇到同类问题的默认解法**：

- 判断标准：这件事需要"给定约束条件、精确计算出一个最优/近似最优
  结果"吗（排班、分摊金额、路径规划这类）？是的话优先写代码，不要
  靠提示词反复调教模型去心算。
- 边界：**只把"计算"这一步挪到代码，不要把"判断"也挪过去**——
  选哪个候选、要不要因为人际因素放弃最优解、怎么组织成一句人话，
  这些仍然是模型的活，不是代码该管的（同样呼应"不定业务规则，让
  大脑看着办"的一贯要求）。写工具时把这个边界说清楚：工具返回候选
  方案和打分依据，不替模型拍板用哪个。
- 这类工具通常不贵：穷举算法本身极快（几毫秒级），比多花几轮提示词
  调教、还调不准，成本低得多。

## ⚠️ 每五次提交做一次 AI 工程体检——速度/成本/聪明程度

**检查目的固定是这三项，每次体检都要对着这三项想一遍**：
**运转速度、token成本、聪明程度**。不是泛泛地"看看有没有能优化的"，
是明确针对这三个维度分别问一遍"这五次改动有没有拖慢它、有没有让它
更费钱、有没有哪里明明能更聪明却没做到"。

**用户明确要求的固定节奏**："请你经常检查。大概每五次提交就检查一次：
AI工程。看看还有什么东西是能大幅改善运转速度、token成本、聪明程度。"

不是等用户问才查，是**自己数着提交数、主动做**。数到第 5 次涉及
`lib/chat/coliving/` 或 `lib/ai/brains/` 的提交（跟文档类提交不算），
主动做一次体检，不需要用户提醒。

**这条故意留模糊，不要把它退化成对照清单打勾就算完事。** 用户原话：
"除了这些，还是让你判断模糊的一句话好了……不要在这个事情省额度。
每五次提交出于那个目的对着五次改动看一看。" ——真正要做的动作是
**认真看这五次改动本身**：读 diff、想一想这五次里有没有引入新的
调用路径、新的循环、新的工具、新的大段文字，**用工程判断力去找问题**，
不是背下面这份清单然后逐条例行公事地扫一遍。清单只是"已经真实踩过
的几类坑，防止健忘漏查"，不是穷尽列表，也不是把体检窄化成只查这
几件事。**这件事不要省额度、不要图快**——宁可多花几次工具调用把
这五次改动认真读一遍，也不要为了走流程随便扫一眼就说没问题。

**已经查过、已知有效的几类问题（下限，不是上限）**：

1. **Prompt cache 断点漏加**（2026-09-04 真实发现过：`critic.ts` 从
   一开始就没开缓存，另外两条今天新加的特殊路径也漏了）。凡是
   `generateText` 调用里带了 `doctrine`/`rubric` 这类逐字不变的大段
   文字，都该有 `providerOptions: { anthropic: { cacheControl: {
   type: "ephemeral" } } }`。新加一条会调用模型的代码路径时，**对照
   已有的主生成调用抄一遍该有的优化**，不要漏。验证方法：连续调
   2-3 次，读 `providerMetadata.anthropic.usage`，第一次应该是
   `cache_creation_input_tokens`，之后应该变成 `cache_read_input_tokens`。
2. **工具数量是否又堆多了**（2026-09-04 真实发现过：21个工具全部
   常驻，"加室友必须打招呼"被挤掉）。查 `turn.ts` 里 `tool({...})`
   的数量，超过 20 个就要重新审视有没有能挪进"情境组"（按
   `loadedModuleIds`/`ctx.openCaseIds` 等信号动态摘取）的候选。
3. **组合优化/精确计算是不是又在靠模型心算**（2026-09-04 真实发现过：
   排班问题）。凡是"给定约束条件、要挑出一个最优/近似最优结果"这类
   任务，检查有没有专门写代码算，而不是靠提示词反复调教。
4. **模型选择是否还是最优**（本会话验证过批判器换便宜模型不划算，
   但**这是当时那几个模型的结论，不是永久结论**——网关新增模型、
   老模型涨价降价，都要重新看一遍 `lib/chat/coliving/model.ts` 和
   `critic.ts` 里写的选型依据是不是还成立）。
5. **上下文是否有不必要的重复计算或冗余膨胀**——`buildContext`/
   `assembleSystemPrompt` 这类每轮该只调一次的，有没有被新代码不小心
   调了第二次；`getRecentTurns` 这类有上限的查询，上限是不是还合适。
6. **工具的 `describe()` 文字是否臃肿**——这个目前是**有意不动**的
   （见下面"组合优化"那条旁边一起写的判断：这些描述文字是这个项目
   反复在修的那类真实事故的免疫力来源，瘦身风险不确定），但值得
   每次体检时留意有没有明显能精简又不影响可靠性的地方。

**体检结果的处理方式**：只报告发现了什么、要不要动、为什么——
不是每次都要真的改代码。找到问题就按平常的流程改（小步验证、
写 AGENT_LOG）；**如果认真看完五次改动之后确实没找到新问题，才可以
说"这次体检没发现新问题"**——但这句话的前提是真的逐个看过、想过，
不是图省事直接下这个结论。不用为了显得有产出硬找茬，但也不能用
"没找到"当省事的借口。

## 评测脚本：按需跑，别当成每次必做的仪式

这些脚本每个都要几分钟到十几分钟（真实模型调用），**不是每次改动都要跑**。
自己判断值不值得；用户说要跑就跑。默认不跑，改完直接说改了什么。

| 脚本 | 盯的是什么 | 什么时候值得跑 |
|---|---|---|
| `pnpm search-eval -- --source wanted --limit 181` | 硬筛选逻辑错误（CODE_BUG） | 动了 `query-plan` / `query-constraints` / 硬筛选谓词 |
| `pnpm search-recall-eval` | "库里有却说没有"（漏召回） | 同上，且怀疑筛严了 —— search-eval 对这一类**结构性失明**，因为它的 ground truth 和运行时共用同一个谓词 |
| `pnpm comment-reply-eval` | 评论成品：角色、格式、字数、幂等 | 动了 comment-reply 链路或提示词 |
| `pnpm redact-eval` | 出站剔联系方式、分割线、字数上限 | 动了 xhs adapter 的出站处理 |
| `pnpm coliving-eval` | 合租房大脑：结构性回归（工具有没有调对、回复/出站消息有没有踩过去的坑）| 动了 `lib/chat/coliving/` 或 `lib/ai/brains/coliving/`。语料在 `lib/chat/coliving/evals/scenarios/`，**只增不减**——抓到新的真实 bug 就整理成一条场景提交进去，见 `docs/coliving-parallel-testing-plan.md` |

场景有两种跑法，**区别很重要**：

- **从零演一遍**（默认）：建空测试屋，把历史消息一条条重新喂进去。
  多轮场景里，中间某一轮新代码反应不一样，后面几轮就跟着分叉——
  最后测的是"新代码对一条被重新演绎的对话的处理"。
- **快照重放**（场景里填 `"snapshot": "<名字>"`）：把某个时刻的完整世界状态
  （人、未结的事、规则、记忆、历史消息、在等谁回话）冻起来恢复成新测试屋，
  **只喂最后那条消息**。没有中间轮次，不分叉，测的就是"新代码对那个
  历史时刻的处理"。抓快照：`pnpm coliving-snapshot -- --list` 看有哪些房子，
  再 `--household <id> --as-of <消息进来的时间> --out <名字>`。
  抓取只读，可以直接对着真实住户的房子跑；恢复永远是新的测试屋、
  号码强制换成测试号段（见 `lib/chat/coliving/evals/snapshot.ts` 开头三条安全性质）。

改一行提示词、修个类型、调个注释——不用跑。真要发布前动过搜索核心逻辑，
再跑对应那一两个。
