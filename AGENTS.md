# WillingLink Codex 工作制度

> 每次新会话先读 `.claude/PROJECT_STATE.md`（项目当前状态单点事实源），再继续。

## 双模型是硬性门禁

这是老板明确要求的长期制度，不是建议，也不能因任务小、赶时间或 Codex 认为自己能完成而省略。

凡是会改变产品行为的实现，包括源码、提示词、测试、数据库迁移、依赖和运行配置：

1. **Claude Code 是唯一实现者。** Codex 必须先把边界清楚的任务交给 Claude Code，由 Claude Code 实际编辑文件。默认调用 `--model sonnet --effort medium`；使用短会话和最小必要上下文节省额度。
2. **Codex 是领导、监工和独立验收者。** Codex 负责调查、拆解、下达任务、阅读完整 diff、运行验证和判断是否达到产品目标。Codex 不得亲自写或补任何实现，哪怕只是一行“小修”。
3. **发现问题必须退回 Claude Code 修改。** Codex 不得把自己的修补交给自己审查，也不得用“先替 Claude 改完”绕过双模型。
4. **Claude Code 不可用就停止。** 无响应、认证失败、额度不足或工具故障时，Codex 必须报告阻塞并修复协作通道；不得悄悄退回单模型实现。只有老板针对当次任务明确授权，才可例外。
5. **验收后才交付。** Codex 验收通过后负责 commit 并 push 到远程 `main`。最终汇报必须分别给出 Claude Code 的实现证据和 Codex 的独立审查/验证证据；没有证据不得声称完成。

下列动作不属于“实现”，Codex 可以直接做：只读调查与诊断、读取历史和数据库、运行测试或检查命令、向 Claude Code 下达任务与反馈、审查 diff，以及 git 提交和推送。对本文件这次建立制度的一次性引导不构成以后自行实现的先例。

**语料转化例外（老板 2026-09-09 明确指定）**：纯对白采集、中文化、人物映射、
对话对象修正和场景 JSON 中 `turns[].text` 的文字转化由 Codex 直接完成，不必交
Claude Code；这类工作不视为写代码。只要涉及运行逻辑、schema、评测脚本、程序断言、
`expect` 机器判定或其他会改变产品行为的部分，仍必须由 Claude Code 实现。

## 项目安全边界

- 先读根目录 `CLAUDE.md`，并遵守其中全部项目背景和安全要求。
- 先读 `.claude/OWNER_TO_CLAUDE.md`（若存在）：那是老板对 Claude Code 说过的话的
  长期记忆，含老板对产品方向、排班、评测、并行测试等历史指令，进入项目先通读，
  避免丢失对老板意图的理解。该文件仅本地保留、不入库。
- 不运行真实 Twilio 发送测试，不向真人发送测试消息，不伪造或污染真实住户历史。
- 本地写入对话只允许测试屋并遵守代码硬闸，绝不绕过。
- `public/sw.js` 是老板的既有未跟踪文件；未经专门要求不得修改、删除或加入提交。
- 保留用户已有的无关改动；每次只提交本任务已验收的文件。

## 测试成本纪律（Vercel AI Gateway 很贵，必须分层省着跑）

模型级评测不能当日常验收用，否则用例越积越多、成本指数级上涨。分层如下，优先用便宜的：

1. **每次改动默认只跑免费闸**：`pnpm.cmd coliving:quality`（离线 46 项确定性检查，
   不调 LLM）+ `pnpm.cmd exec tsc --noEmit` + `git diff --check`。这一层能覆盖
   结构性回归，绝大多数改动到这里就够。
2. **模型级 `coliving-eval` 只在必要时跑，且尽量省**：
   - 用 `--scenario <id>` 只跑受影响的场景，不默认跑全量。
   - 用 `--judge-off` 跳过最贵的语义 judge（sonnet），只跑免费的结构断言。
   - 被测大脑保持生产默认 `deepseek-v4-flash`，不要为“更聪明”临时切 sonnet 烧钱。
3. **全量 `coliving-eval`（含语义 judge）只用于**：发布前、或老板明确要求；
   一次跑完就记录结果，不重复跑同一批。
4. 新增语料只增不减，但“用例数”与“每次要跑的规模”解耦：新用例进库不等于每次
   改动都要全量重跑；结构层先拦，语义层按需、抽样。

这组纪律长期有效，不因单次任务“想更稳”就破例烧钱。
